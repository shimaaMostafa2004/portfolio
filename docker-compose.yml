services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${CONTAINER_NAME:-portfolio-app}
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:${PORT:-8080}"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=${PORT:-8080}
